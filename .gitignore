# Temporary Bundles
temp_bundle_*
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor/IDE
.vscode/
.idea/
*.iml
*.swp
*.swo

# Node / Electron
node_modules/
**/node_modules/
yarn.lock
# keep lockfiles if you prefer deterministic installs; comment out above line to track yarn.lock
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
.parcel-cache/
.next/
.nuxt/
# Electron builder outputs in individual app folders (use root dist/ instead)
bundler_app/dist/
comparison_app/dist/
desktop_matching_app/dist/
out/
**/out/
*.asar
# Electron builder outputs
release/
**/release/

# Root dist folder structure (only final installers)
dist/
# But track the folder structure
!dist/.gitkeep
!dist/bundler/.gitkeep
!dist/comparison/.gitkeep
!dist/desktop-matching/.gitkeep
!dist/mobile/.gitkeep

# Flutter / Dart
.dart_tool/
.packages
.flutter-plugins
.flutter-plugins-dependencies
.metadata
.melos_tool/
.fvm/
coverage/
# Flutter/Gradle build outputs
build/
**/build/
# Exception: Allow installer assets in electron app build dirs
!bundler_app/build/installer*.{ico,png,nsh}
!comparison_app/build/installer*.{ico,png,nsh}
!desktop_matching_app/build/installer*.{ico,png,nsh}
# But block all executables and large files
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.snap
*.msi

# Large binary dependencies (>25MB)
win32-resources/ffmpeg-static.zip

# Android / Gradle
.gradle/
**/.gradle/
**/local.properties
**/captures/
**/generated/
**/android/app/release/
# Keystores (never commit secrets)
*.jks
*.keystore

# Java/Kotlin
*.class
hs_err_pid*
replay_pid*

# Logs
*.log
logs/
**/logs/

# Misc
.env
.env.*
.env.local
.sass-cache/
.eslintcache
.cache/
.tmp/
.temp/
Thumbs.db

# Visual Studio Code Workspace Settings
*.code-workspace
.vscode/
vs_code.json

private